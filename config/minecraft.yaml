# ==========================================================
# Minecraft Server & Bot Configuration for muu-mc
# ==========================================================

server:
  host: "127.0.0.1"
  port: 25565
  version: false # false = auto-detect server version dynamically (1.16 - 1.21+)
  auth: "offline" # offline (cracked/local) or microsoft

bot:
  username: "Muumiu"
  view_distance: "far"
  check_timeout_interval: 30000
  physics:
    sprint: true
    sneak: false

viewer:
  enabled: true
  port: 3007
  first_person: true

auto_reconnect:
  enabled: true
  max_retries: 10
  retry_delay_ms: 5000
